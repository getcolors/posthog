{:paths ["src/clj" "src/resources"]
 :deps {org.clojure/clojure       {:mvn/version "1.12.5"}
        io.github.getcolors/green {:git/url "https://github.com/getcolors/green.git"
                                   :git/sha "ceb4159a19ec281d60f10d11295c05c5de5f1c42"}
        ;; 417d5f7 is where ONCE gained `compute`, the Compute Provider
        ;; Standard's operations this package delegates to (eea43c2, the pin,
        ;; quotes a malformed CIDR entry identically in every colour); bc06f2f,
        ;; below both, is where ONCE moved the machine keypair into the
        ;; operator's ~/.ssh. The pin cannot go below any of them.
        io.github.getcolors/once  {:git/url "https://github.com/getcolors/once.git"
                                   :git/sha "eea43c2fe0d8c1d45d07ccfee41edc81ea4c7c6e"
                                   :deps/root "green"}}
 :aliases {:dev {:extra-paths ["test/clj" "test/resources"]}
           :test {:extra-paths ["test/clj" "test/resources"]
                  :extra-deps {io.github.cognitect-labs/test-runner
                               {:git/tag "v0.5.1" :git/sha "dfb30dd"}}
                  :exec-fn cognitect.test-runner.api/test}}}
